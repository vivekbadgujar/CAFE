# Frontend (existing)

This folder is reserved for the existing React + Vite frontend. Clone the repository into this directory:

```bash
git clone https://github.com/shaileshnai8-eng/goodgrounds1.git frontend
```

After cloning, configure `frontend/.env` with `VITE_API_URL` for API integration.
